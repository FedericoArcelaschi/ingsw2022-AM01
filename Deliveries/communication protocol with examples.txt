(to view with tab size 4.)
Comunication Protocol:

+---------------+												+---------------+
|Client			|												|Server			|
+---------------+												+---------------+
		|																| SERVER IS LISTENING on PORT ????
		|																|
		|																|
		|	*FIRST MESSAGE*												|
		|	------------------------------------------------------->	| Answers the first message, opens the Socket and assignes 
		|	<-------------------------------------------------------	| an unique ID to the client.
		|																|
		|																|
		|																|
		|													  *PING*	|
		|	<-------------------------------------------------------	| Heartbeat protocol:
		|	*PONG*														| The server every 10 seconds sends a Ping message to the client
		|	------------------------------------------------------->	| and waits for a Pong message. If the client doesn't answer within
		|												*DISCONNECT*	| 10 seconds the Server sends a disconnect message?
		|	< - - - - - - - - - - - - - - - - - - - - - - - - - - - 	|
		|																|
		|																|
		|																|
		|								 *BEGIN OF GAME (broadcast)* 	| If there is a game the player can join, assignes
		|	<-------------------------------------------------------	| the player with his peers to a game and sends
		|	<-------------------------------------------------------	| the game view to all the players in that game.
		|																|
		|																|
		|											*ACTION TO MAKE*	| When is his turn, the server sends to the player a prompt to
		|	<-------------------------------------------------------	| make an action
		|	(e.g.: It's your turn to play a card)						|
		|																|
		|																|
		|																|
		|	*LEGAL ACTION*												|
		|	------------------------------------------------------->	| The server receives a legal command from the client.
		|	(e.g.: Move GREEN to Castle)*UPDATE TO VIEW (broadcast)*	| The server updates the model and each time it sends
		|	<-------------------------------------------------------	| the updated view to all the clients in the game.
		|	<-------------------------------------------------------	|
		|																|
		|																|
		|																|
		|	*ILLEGAL ACTION*											| In case a player sends an illegal move we don't update the model
		|	------------------------------------------------------->	| and the server sends only to him an error message.
		|	(e.g: Move MotherNature 7)				 *ERROR MESSAGE*	| Beacause the game has predefined turns and phases we'll wait for
		|	< - - - - - - - - - - - - - - - - - - - - - - - - - - - 	| each player to send a legal action before we can proced with the
		|																| game-play
